import Mock from 'mockjs'

export const getUserInfo = (options) => {
  // const template = {  // mock中可以使用模板
  //   'str[2-4]': 'likai'
  // }
  // return Mock.mock(template);
  console.log(options);
}